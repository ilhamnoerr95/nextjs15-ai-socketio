## Group router

this router group new feature next js 15 for app router that, folder (auth) will not be a prefix or parent of route children,
this will make easier for router structure.

so we can through with [localhost:3000/login](localhost:3000/login) not [localhost:3000/auth/login](localhost:3000/auth/login)
